#include "main.h"

/**
 * print_sign - Shows whether it is positive, 0 or negative
 * @c: Random number
 * Return: 0
 */

int print_sign(int n)
{
	if (c > 0)
	{
		_putchar('+');
		return (1);
	}
	else if (c < 0)
	{
		_putchar('-');
		return (-1);
	}
	else
	{
		_putchar('0');
		return (0);
	}
}
